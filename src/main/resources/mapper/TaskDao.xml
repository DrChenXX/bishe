<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bishe.dao.TaskDao">

    <select id="getAll" resultType="com.example.bishe.entity.TaskEntity">
        select * from task
    </select>

    <insert id="add" parameterType="com.example.bishe.entity.TaskEntity">
        insert into task(id, farm_id, deadline, pic_id, money, worker_id) VALUES (#{id},#{farmId},#{deadline},#{picId},#{money},#{workerId})
    </insert>

    <update id="update">
        update task set farm_id = #{farmId}, deadline = #{deadline}, pic_id = #{picId}, money = #{money}, worker_id = #{workerId} where id = #{id}
    </update>

    <delete id="delete">
        delete from task where id = #{id}
    </delete>

</mapper>