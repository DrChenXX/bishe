<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bishe.dao.SalaryDao">

    <select id="searchByCondition" parameterType="com.example.bishe.entity.SalaryEntity" resultType="com.example.bishe.entity.SalaryEntity">
        select * from salary
        <where>
            <if test="id != 0">
                and id = #{id}
            </if>
            <if test="workerId != 0">
                and worker_id = #{workerId}
            </if>
        </where>
    </select>

    <insert id="add" parameterType="com.example.bishe.entity.SalaryEntity">
        insert into salary(id, worker_id, time, number, note) VALUES (#{id},#{workerId},#{time},#{number},#{note})
    </insert>

    <update id="update">
        update salary set worker_id = #{workerId}, time = #{time}, number = #{number}, note = #{note} where id = #{id}
    </update>

    <delete id="delete">
        delete from salary where id = #{id}
    </delete>

</mapper>