<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bishe.dao.WorkerDao">

    <select id="getAll" resultType="com.example.bishe.entity.WorkerEntity">
        select * from worker
    </select>

    <insert id="add" parameterType="com.example.bishe.entity.WorkerEntity">
        insert into worker(id, name, farm_id, bank, self_id) VALUES (#{id},#{name},#{farmId},#{bank},#{selfId})
    </insert>

    <update id="update">
        update worker set name = #{name}, farm_id = #{farmId}, bank = #{bank}, self_id = #{selfId} where id = #{id}
    </update>

    <delete id="delete">
        delete from worker where id = #{id}
    </delete>

</mapper>