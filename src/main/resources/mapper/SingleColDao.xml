<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bishe.dao.SingleColDao">

    <select id="getAll" resultType="com.example.bishe.entity.SingleColEntity">
        select * from single_col
    </select>

    <insert id="add" parameterType="com.example.bishe.entity.SingleColEntity">
        insert into single_col(id, worker_id, pic_id, age, circum, towards, time, weather, location, state, note) VALUES (#{id},#{workerId},#{picId},#{age},#{circum},#{towards},#{time},#{weather},#{location},#{state},#{note})
    </insert>

    <update id="update">
        update single_col set worker_id = #{workerId}, pic_id = #{picId}, age = #{age}, circum = #{circum}, towards = #{towards}, time = #{time}, weather = #{weather}, location = #{location}, state = #{state}, note = #{note} where id = #{id}
    </update>

    <delete id="delete">
        delete from single_col where id = #{id}
    </delete>

</mapper>