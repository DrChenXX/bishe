<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bishe.dao.MessageDao">

    <select id="searchByCondition" parameterType="com.example.bishe.entity.MessageEntity" resultType="com.example.bishe.entity.MessageEntity">
        select * from message
        <where>
            <if test="id != 0">
                and id = #{id}
            </if>
            <if test="noticeId != 0">
                and notice_id = #{noticeId}
            </if>
            <if test="workerId != 0">
                and worker_id = #{workerId}
            </if>
        </where>
    </select>

    <insert id="add" parameterType="com.example.bishe.entity.MessageEntity">
        insert into message(id, notice_id, worker_Id, `read`) VALUES (#{id},#{noticeId},#{workerId},#{read})
    </insert>

    <update id="update">
        update message set notice_id = #{noticeId}, worker_Id = #{workerId}, 'read' = #{read} where id = #{id}
    </update>

    <delete id="delete">
        delete from message where id = #{id}
    </delete>

</mapper>