<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bishe.dao.AppealDao">

    <select id="getAll" resultType="com.example.bishe.entity.AppealEntity">
        select * from appeal
    </select>

    <insert id="add" parameterType="com.example.bishe.entity.AppealEntity">
        insert into appeal(id, type, single_id, reason, state) VALUES (#{id},#{type},#{singleId},#{reason},#{state})
    </insert>

    <update id="update">
        update appeal set type = #{type}, single_id = #{singleId}, reason = #{reason}, state = #{state} where id = #{id}
    </update>

    <delete id="delete">
        delete from appeal where id = #{id}
    </delete>

</mapper>